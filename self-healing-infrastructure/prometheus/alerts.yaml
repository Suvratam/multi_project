groups:
- name: example
  rules:
  - alert: NginxHttpDown
    expr: probe_success{job="blackbox"} == 0
    for: 1m
    labels:
      severity: critical
    annotations:
      summary: "NGINX HTTP endpoint down"
      description: "NGINX HTTP endpoint is not responding for more than 1 minute."
